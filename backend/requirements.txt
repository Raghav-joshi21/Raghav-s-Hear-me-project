fastapi
uvicorn[standard]
gunicorn
pydantic
numpy
python-multipart
# For inference only, not model training (use only if imported)
tensorflow
# Azure SDKs actually imported in code:
azure-communication-identity
azure-communication-rooms
